Модуль хранения данных

Функция save_history принимает строку с операцией и результат и дописывает их в файл history.txt. Ничего не возвращает.

Функция load_history не принимает ничего и возвращает список всех сохранённых строк из history.txt. Если файла нет возвращает пустой список
